import { styled } from '@mui/material'

// Styles for Login For divider left side
const DivStyles = styled('div')({
    display: 'flex', alignItems: 'center', justifyContent: 'space-around', 
})
export default DivStyles

